From 15440c95b52327235f528a1d2ea701177f90c36c Mon Sep 17 00:00:00 2001
From: SVN-Git Migration <python-modules-team@lists.alioth.debian.org>
Date: Thu, 8 Oct 2015 14:10:01 -0700
Subject: add_support_for_DEBEMAIL

Patch-Name: add_support_for_DEBEMAIL.patch
---
 stdeb/command/common.py | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

diff --git a/stdeb/command/common.py b/stdeb/command/common.py
index 93a3a5b..9cf6e0d 100644
--- a/stdeb/command/common.py
+++ b/stdeb/command/common.py
@@ -91,7 +91,11 @@ class common_debian_package_command(Command):
 
         if 1:
             # set default maintainer
-            if (self.distribution.get_maintainer() != 'UNKNOWN' and
+            if os.environ.get('DEBEMAIL'):
+                guess_maintainer = "%s <%s>" % (os.environ.get('DEBFULLNAME',
+                                                               os.environ['DEBEMAIL']),
+                                                os.environ['DEBEMAIL'])
+            elif (self.distribution.get_maintainer() != 'UNKNOWN' and
                 self.distribution.get_maintainer_email() != 'UNKNOWN'):
                 guess_maintainer = "%s <%s>"%(
                     self.distribution.get_maintainer(),
